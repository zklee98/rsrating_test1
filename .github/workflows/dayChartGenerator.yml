name: dayChartGenerator

on:
  #push:
    #branches:
  schedule:
    - cron: "0 4 * * 6"

  workflow_dispatch:

jobs:
      build:
        runs-on: ubuntu-latest

        steps:
          - name: checkout repo content
            uses: actions/checkout@v2 # checkout the repository content to github runner.
              
          - name: setup python
            uses: actions/setup-python@v2
            with:
              python-version: 3.10.12 #install the python needed
              
          - name: execute py script # aggregate current data
            run: |
              python -m pip install -r requirements1.txt'
              python plot-day-chart.py 



//////////////bottom not configured yet. continue here

